#include "main.h"
/**
 * print_last_digit - prints last digit of a given number
 * @int: parameter
 * Return: always success
 */
int print_last_digit(int n)
{
	int last;
	last = n % 10;

	return (last)
}
